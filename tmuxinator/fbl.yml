name: fb-landing
root: /Users/andreasbade/repos/fb-landing

windows:
  - dev:
      layout: tiled
      panes:
        - server:
          - cd .
          - hugo server --baseURL="finderbox.dev" --buildDrafts
        - dev:
          - cd .
