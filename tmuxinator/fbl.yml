name: fb-landing
root: <%= ENV["FBX_HOME"] %>/fb-landing

windows:
  - dev:
      layout: tiled
      panes:
        - server:
          - cd .
          - hugo server --baseURL="finderbox.dev" --buildDrafts
        - dev:
          - cd .
